/**
 * Types et constantes partagés entre l'API et l'application mobile.
 *
 * Phase 1 (Fondation) : squelette du package uniquement. Les types de
 * domaine (Household, Item, Category, ...) seront ajoutés en Phase 2,
 * en miroir du schéma Prisma et du contrat OpenAPI.
 */

export const APP_NAME = 'Notre Nid' as const;
