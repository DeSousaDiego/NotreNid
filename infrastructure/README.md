# infrastructure/

Réservé aux artefacts d'infrastructure de production (Dockerfile multi-stage, configuration de déploiement, scripts de sauvegarde `pg_dump`, ...) introduits en Phase 5 — Livraison. Voir `docs/IMPLEMENTATION_PLAN.md`.

Le développement local utilise le `docker-compose.yml` à la racine du dépôt, pas ce dossier.
